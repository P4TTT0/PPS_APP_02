import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionica.starter',
  appName: 'Jugar',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
